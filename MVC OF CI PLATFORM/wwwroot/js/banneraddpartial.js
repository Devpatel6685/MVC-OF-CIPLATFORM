var fileInput = $('#imageInput')$("#showImage img").width(200).height(150);fileInput.change(function (event) {    var file = event.target.files;    var reader = new FileReader();    reader.onload = function (event) {        $('#showImage img').attr('src', event.target.result);        $("#showImage img").width(200).height(150);    };    reader.readAsDataURL(file[0]);})$('#form').submit(function (event) {    event.preventDefault();    if ($('#showImage img').prop('src') == '') {        $('#imageValidate').removeClass('d-none');    } else {        $('#imageValidate').addClass('d-none');        $('#form').submit();    }});tinymce.init({    selector: '#storytext',    plugins: 'link image code',    toolbar: 'undo redo | bold italic | fontsizeselect | alignleft aligncenter alignright alignjustify | superscript subscript ',    height: 300});